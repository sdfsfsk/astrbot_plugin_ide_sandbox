向用户提问并把问题发送到群聊。

使用场景：
- 需求不明确，需要用户澄清。
- 多个可选方案，需要用户选择。
- 需要用户确认某个操作（如删除文件、覆盖配置）。

Tips:
- 问题要简洁明确，避免一次性问太多问题。
- 不要滥用本工具， trivial 决策应直接执行。

Args:
    question(string): 要向用户提出的问题。

Returns:
    确认信息，提示已发送问题。
